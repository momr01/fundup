CREATE PROCEDURE [dbo].[FinalizarPlan]
	@IdUsuario int,
	@ID_PLAN int
AS
BEGIN
	SET NOCOUNT ON;
	UPDATE [dbo].[PLAN]
	SET 
	ESTA_COMPLETO_PLAN = 1,
	FECHA_COMPLETO = GETDATE()
	WHERE ID_PLAN = @ID_PLAN
	AND ID_USUARIO = @IdUsuario
END